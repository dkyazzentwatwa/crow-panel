# Codex Build Notes

Proof targets:

- `scaffolded`: all BadgeOps files exist
- `compile-ready`: Arduino CLI compile succeeds with mock mode
- `uploaded`: upload succeeds to a detected CrowPanel serial port
- `field-proven`: badge flow is observed on the physical display or Serial monitor

Do not claim secure access control. UID-only RFID/NFC is a demo boundary unless stronger credential design and backend validation are added.
