#include "BadgeReader.h"

// BadgeReader is an abstract interface. This file is intentionally present so
// the project tree mirrors the tutorial structure and Arduino CLI has a normal
// translation unit for the reader boundary.
