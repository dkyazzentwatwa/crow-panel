# Codex Build Notes

Proof targets:

- `scaffolded`: all Vision Guard files exist
- `compile-ready`: Arduino CLI compile succeeds with mock mode
- `uploaded`: upload succeeds to a detected CrowPanel serial port
- `field-proven`: kiosk output is observed on the physical display or Serial monitor

Do not claim camera capture until the official Elecrow camera example runs on the real board.
