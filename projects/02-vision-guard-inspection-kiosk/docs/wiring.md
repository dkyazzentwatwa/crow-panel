# Vision Guard Wiring Notes

Camera wiring is intentionally not assigned here.

Before enabling `USE_CAMERA_DRIVER`:

- Confirm the CrowPanel hardware revision.
- Confirm the camera module and connector.
- Find the matching official Elecrow Arduino example.
- Copy verified values into a local pins/config file.

The mock demo does not require camera hardware.
